/**
 * 交付中心模块入口（Phase 3）。
 *
 * 当前只导出 `DeliveryCenter`。Phase 5+ 之后这里可能会增加：
 *   - DeliveryCenter（已在）
 *   - DeliveryListView（聚合 ServiceInstance 的跨模块列表，Phase 5）
 *   - PersonArchiveList / PersonArchiveDetail（对象档案，Phase 6）
 */
export { DeliveryCenter } from './DeliveryCenter';
